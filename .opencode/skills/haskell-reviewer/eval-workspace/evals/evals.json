{
  "skill_name": "haskell-reviewer",
  "note": "Seed suite. Cases embed code to review and check that the review flags the real issues (and, for case 2, does NOT invent spurious ones). Maintainers should refine assertions and add cases per CONTRIBUTING.md.",
  "evals": [
    {
      "id": 0,
      "name": "review-planted-correctness-and-perf",
      "prompt": "Please review this Haskell module.\n\n```haskell\nmodule Worker where\n\nimport Prelude\n\ndata Config = Config { configHost :: String, configRetries :: Int }\n\nsumFile :: FilePath -> IO Int\nsumFile path = do\n  contents <- readFile path\n  let ns = map read (lines contents)\n  pure (foldl (+) 0 ns)\n\nfirstLine :: FilePath -> IO String\nfirstLine path = do\n  contents <- readFile path\n  pure (head (lines contents))\n```",
      "expected_output": "Flags: partial functions head and read in production; lazy foldl -> foldl'; lazy Prelude.readFile -> Data.Text.IO/ByteString; String field should be Text; non-strict Config fields (missing !). Bonus: organizes by severity (Bugs/Performance/Design/Style) with concrete fixes.",
      "files": [],
      "assertions": [
        "Flags the partial functions `head` and `read` used in production code",
        "Flags the lazy `foldl` and recommends `foldl'`",
        "Flags lazy `Prelude.readFile` and recommends Data.Text.IO / ByteString equivalents",
        "Flags `String` usage (e.g. configHost) and recommends `Text`",
        "Flags the non-strict `Config` fields (missing `!`)",
        "Bonus: organizes findings by severity with concrete suggested fixes"
      ]
    },
    {
      "id": 1,
      "name": "review-transformer-stack-and-env",
      "prompt": "Review the application-monad design in this module.\n\n```haskell\nmodule App where\n\nimport Control.Monad.Except\nimport Control.Monad.State\nimport Control.Monad.Reader\n\ndata Env = Env\n  { envPool  :: Pool Connection\n  , envUsers :: IORef [User]\n  }\n\nnewtype App a = App (ExceptT Err (StateT Cache (ReaderT Env IO)) a)\n  deriving (Functor, Applicative, Monad)\n```",
      "expected_output": "Flags the deep transformer stack -> newtype over ReaderT Env IO; StateT -> IORef/TVar in env; ExceptT -> exceptions in IO caught at boundaries; mutable business state in env (envUsers :: IORef [User]) -> env should hold resources, not business data; non-strict env fields. Bonus: Has-pattern / runApp.",
      "files": [],
      "assertions": [
        "Flags the deep transformer stack and recommends a newtype over `ReaderT Env IO`",
        "Flags `StateT` and recommends an IORef/TVar in the environment instead",
        "Flags `ExceptT` and recommends throwing exceptions in IO, caught at boundaries",
        "Notes that an explicit error type should default to ExceptT String / Either String rather than a bespoke error ADT unless matched on for flow control",
        "Flags mutable business state in the env (envUsers :: IORef [User]) — env should hold resources, not business data",
        "Bonus: notes non-strict env fields and/or the Has-pattern / missing runApp"
      ]
    },
    {
      "id": 2,
      "name": "review-idiomatic-no-false-positives",
      "prompt": "Review this module.\n\n```haskell\nmodule Domain.User where\n\nimport qualified Data.Text as T\nimport Data.Text (Text)\nimport Data.Int\n\nnewtype UserId = UserId { unUserId :: Int64 }\n  deriving newtype (Eq, Ord, Show)\n\ndata Role = Admin | Editor | Viewer\n  deriving stock (Eq, Ord, Show)\n\nnewtype Email = Email { unEmail :: Text }\n  deriving stock (Eq, Show)\n\nmkEmail :: Text -> Either Text Email\nmkEmail t\n  | T.isInfixOf (T.pack \"@\") t = Right (Email t)\n  | otherwise                 = Left (T.pack \"invalid email\")\n\ndata User = User\n  { _user_id    :: !UserId\n  , _user_role  :: !Role\n  , _user_email :: !Email\n  }\n  deriving stock (Eq, Show)\n```",
      "expected_output": "This code already follows the skill's guidance (newtype ids, sum type for Role, smart constructor for Email, strict type-prefixed fields, explicit DerivingStrategies, Text not String). A good review recognizes it is largely idiomatic and does NOT invent spurious bugs; any suggestions are minor/genuine, not fabricated problems.",
      "files": [],
      "assertions": [
        "Recognizes the code is largely idiomatic and correct (does not claim it is broken)",
        "Does NOT fabricate spurious correctness/performance issues (no false flags about partiality, foldl, String, etc. that aren't present)",
        "Any suggestions offered are genuine, minor improvements rather than nitpicks or invented problems"
      ]
    }
  ]
}
